# Entrada de dados nome_vendedor = 
input( "Digite o nome do vendedor:")
salario_fixo = float(input("Digite o salàrio fixo do vendedor:"))

total_vendas = float (int) (input("Digite o salario fixo do vendedor:"))

#cálculo da comissão bonus = 0.15*salario_fixo

# cálculo do sálario final

Salario_recebido = salario_fixo + "bonus" if total_vendas >= 20 else salario_fixo

#verificação da meta e exibição dos resultados 
if total_vendas >= 20: print(f"Parabéns){"Nome_vendedor"} ! você atingiu a meta.")
print(f"salario final:R$")
(f"salario_recebido:.2f")
print(f"nome_vendedor"),"você não atingiu a meta"
print(f"salário_recebido: R$ ")
("")